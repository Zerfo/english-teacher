import { BrowserRouter } from "react-router-dom";

import Navigator from "containers/navigator";

function App() {
  return (
    <BrowserRouter>
      <Navigator />
    </BrowserRouter>
  );
}

export default App;
